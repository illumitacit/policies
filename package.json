{
  "name": "illumitacit-policies",
  "version": "1.0.0",
  "description": "Policies site for IllumiTacit",
  "repository": "git@github.com:illumitacit/policies.git",
  "author": "IllumiTacit <support@illumitacit.com>",
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "parcel build && hugo --gc --minify --enableGitInfo",
    "build:preview": "parcel build && hugo --gc --minify --buildFuture -b $DEPLOY_PRIME_URL"
  },
  "targets": {
    "default": {
      "source": "assets/index.js",
      "distDir": "assets/dist"
    }
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "parcel": "^2.8.3",
    "postcss": "^8.4.21",
    "tailwindcss": "^3.3.1"
  }
}
